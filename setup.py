"""
Setup script for epipelagic-ns package.

For development installation:
    pip install -e .

For installation with all optional dependencies:
    pip install -e ".[all]"
"""

from setuptools import setup

# Configuration is in pyproject.toml
setup()
